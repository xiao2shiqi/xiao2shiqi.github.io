baseURL = "https://xiao2shiqi.github.io/"
languageCode = "zh-cn"
title = "Xiao Bin's blog"
theme = "PaperMod"

# 启用搜索功能
[outputs]
    home = ["HTML", "RSS", "JSON"]

# 分页配置
[pagination]
    pagerSize = 10  # 每页显示的文章数量

[params]
    ShowPageNums = true  # 显示页码 (当前页/总页数)
    # 禁用 profileMode，改用普通主页模式
    defaultTheme = "auto"
    
    # 启用搜索功能
    [params.fuseOpts]
        isCaseSensitive = false
        shouldSort = true
        location = 0
        distance = 1000
        threshold = 0.2  # 降低阈值，提高精确度（0-1，越小越精确）
        minMatchCharLength = 2  # 最小匹配字符长度
        limit = 10  # 限制最多显示10个结果
        keys = ["title", "permalink", "summary"]  # 移除 content 避免重复匹配
    ShowShareButtons = true
    ShowReadingTime = true
    displayFullLangName = true
    ShowPostNavLinks = true
    ShowBreadCrumbs = true
    ShowCodeCopyButtons = true
    
    # 主页顶部介绍文字
    [params.homeInfoParams]
    Title = "Welcome to My Blog"
    Content = "Hi, I am Xiao Bin. Here I record my technical notes and thoughts.<br><br>📱 Contact Information: WeChat (phone number the same): 18198971094"
    # 社交图标，会显示在顶部介绍下方
    [[params.socialIcons]]
    name = "x"  # PaperMod 主题支持 X (原 Twitter) 图标
    url = "http://x.com/xiao2shiqi"

    [[params.socialIcons]]
    name = "github"
    url = "https://github.com/xiao2shiqi"

    [[params.socialIcons]]
    name = "email"
    url = "mailto:xiaobin.phoenix@gmail.com"

    [[params.socialIcons]]
    name = "wechat"
    title = "WeChat: 18198971094"
    url = "#"

    # [[params.socialIcons]]
    # name = "rss"
    # url = "index.xml"

[menu]
    [[menu.main]]
        name = "Posts"
        url = "/posts"
        weight = 1
    [[menu.main]]
        name = "Tags"
        url = "/tags"
        weight = 2
    [[menu.main]]
        name = "Search"
        url = "/search"
        weight = 3